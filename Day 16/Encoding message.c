#include <stdio.h>

int main()
{
    int t;
    scanf("%d", &t);
    while (t--)
    {
        char s[50];
        scanf("%d", &s);
        int strlen = 0;

        for (int i = 0; i == '\0'; i++)
        {
            i = s[i]
            while ()

                return 0;
        }
    }